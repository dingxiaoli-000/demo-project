# 页面实现状态

## 01 Header Hero

- 状态：框架、正式主视觉视频、静态 poster 和品牌 Logo 已接入，等待后续像素级精修。
- 实现文件：`index.html`、`style.css`、`main.js`、`assets/icons.svg`
- 正式主视觉路径：`assets/images/hero-brain.png`（1520 × 1520，透明 PNG）。
- 主动画路径：`assets/videos/hero-transparent.webm`（960 × 960，约 2.7MB，VP9 Alpha，无音轨）。
- 兼容回退：`assets/videos/hero.mp4`（约 4.5MB）；仅回退时使用 darken 混合与边缘遮罩。
- 品牌 Logo 路径：`assets/images/logo.svg`。
- 已实现：固定胶囊导航、品牌标识、桌面导航、移动菜单、主标题、说明文字、双 CTA、双层轨道、轨道节点、五个 Agent 标签。
- 图标系统：Agent 标签已切换为本地化 Phosphor Icons Regular SVG，映射见 `docs/ICON_SYSTEM.md`。
- 视觉精修：桌面导航高度为 68px；五个 Agent 标签使用无描边、高不透明度、24px 模糊的玻璃背景，图标统一使用页面主色。
- 按钮交互：两个首屏 CTA 悬停或键盘聚焦时保持背景颜色不变，箭头向右移动 3px，并保留 2px 上浮反馈。
- 响应式：桌面双栏；900px 以下上下排列；620px 以下按钮全宽并重排标签。
- 已验证视口：1440 × 900、390 × 844。
- 验证结果：无横向溢出；图片加载正常；移动菜单交互正常；控制台无错误。

## 09 Footer

- 状态：响应式语义框架已完成，等待全页像素级精修。
- 已实现：品牌简介、三枚社交入口、产品/核心模块/场景/资源/联系五列导航、版权信息及隐私政策/服务条款/数据安全入口。
- 视觉事实：桌面端左品牌右五列、版权位于左下、条款位于右下的构图关系来自参考裁图。
- 响应式：平板压缩列宽；手机端品牌居中、导航两列排列，联系栏横跨整行，底部法律信息与版权分层展示，属于实现推断。
- 实现推断：参考图中小字号说明和部分链接无法完全辨认，当前按可辨语义补写；链接目的地仍为框架级锚点。

## 08 Bottom CTA

- 状态：响应式语义框架与未来城市正式高清配图已完成。
- 已实现：超宽大圆角 CTA 容器、左侧标题与副文案、预约产品演示/获取解决方案双按钮、右侧未来城市固定素材槽位及缺图降级层。
- 视觉事实：左文右图、浅蓝白玻璃容器、蓝紫渐变主按钮、浅色描边次按钮、环形未来城市构图均来自参考裁图。
- 响应式：桌面保持左文右图；平板压缩配图并保留双栏；手机改为文案、双按钮同行、城市图的纵向布局，属于实现推断。
- 正式素材：用户提供的 `11.png` 已语义化重命名为 `assets/images/bottom-cta-city.png`（1464 × 864 RGBA PNG）并接入。
- 批注调整：标题与说明文字间距为 `8px`；小屏端双 CTA 按钮保持单排；两个 CTA 统一为胶囊椭圆按钮。

## 07 Whitepapers

- 状态：响应式语义框架与四张正式配图已完成，等待全页像素级精修。
- 已实现：大圆角资源容器、左侧立体书封、区块标题与副标题、三张白皮书资源卡、书签、内容预览、在线浏览与下载按钮。
- 结构：标题区独立居中；下方内容盒子统一包含图书配图和三张资源卡。书封使用混合模式与边缘渐隐融入背景。
- 响应式：桌面端左书右卡，两个子区间距为 `24px`；平板/手机端按标题、图书、资源卡的顺序排列，资源卡可横向滑动且不显示滚动条。
- 正式配图：`whitepaper-book.png`、`whitepaper-agent-architecture.png`、`whitepaper-procurement-flow.png`、`whitepaper-cost-chart.png` 已按语义接入。
- 实现推断：部分小文案按参考图可辨语义补写。

## 06 Case Study

- 状态：响应式语义框架与正式项目鸟瞰图已完成，等待像素级精修。
- 实现文件：`index.html`、`style.css`、`main.js`。
- 已实现：居中标题、左侧项目图槽位、案例标题与标签、双段说明、四项成效指标、六节点执行时间轴，以及桌面双栏、平板纵向、手机纵向时间轴布局。
- 视觉事实：单张大案例卡、左图右文、四指标卡和横向时间轴来自参考裁图。
- 正式素材：用户提供的数字序号 `1.png` 已语义化重命名并接入为 `assets/images/case-study-aerial.png`（1254 × 1254，不透明 PNG）；页面仍保留缺图降级显示。
- 实现推断：项目说明、部分时间轴文字及移动端重排按可辨语义完成，待品牌原始文案确认。

## 05 Intelligent Modules

- 状态：响应式语义框架与五张正式透明配图已接入，等待全页像素级精修。
- 实现文件：`index.html`、`style.css`。
- 已实现：居中标题、五张等宽模块卡、模块标题、三条功能要点、“了解更多”链接，以及桌面五列、平板两列、手机单列布局。
- 间距规范：模块卡四边内边距统一为 `16px`；功能列表与“了解更多”链接间距为 `12px`，链接不再自动沉底。
- 视觉事实：五列卡片构图、蓝色透明 3D 图标、功能列表和底部紫色链接均来自参考裁图。
- 实现推断：功能要点根据参考图可辨内容整理，待品牌原始文案确认。
- 正式配图：`module-smart-procurement.png`、`module-weighing-control.png`、`module-smart-finance.png`、`module-wecom-agent.png`、`module-bim-materials.png`，均为 500 × 500 RGBA PNG。

## 04 Platform Architecture

- 状态：响应式语义框架与六张正式透明配图已接入，等待像素级精修。
- 实现文件：`index.html`、`style.css`。
- 已实现：居中标题、左侧双环工程大脑中枢、六个业务 Agent 胶囊标签、右侧五层平台架构卡片、中央正式 3D 配图、五层正式 3D 图标，以及桌面双栏、平板/手机纵向响应式。
- 标签质感：六个 Agent 胶囊使用低透明度白色背景与背景模糊，保留轻微悬浮阴影，不添加额外高光或折射装饰。
- 标签构图：六个 Agent 胶囊已向中央主图适度聚拢，桌面与手机端分别校准定位，保持轨道环绕关系。
- 视觉事实：标题、左侧环形中枢与六标签构图、右侧五层纵向卡片、蓝紫主色和白色半透明卡片关系均来自参考裁图。
- 实现推断：右侧说明文字及第六个 Agent 名称因原图清晰度有限按业务语义补写，待确认。
- 正式配图：`platform-architecture-core.png` 为 1520 × 1520 RGBA PNG；`architecture-data-layer.png`、`architecture-analysis-layer.png`、`architecture-agent-layer.png`、`architecture-execution-layer.png`、`architecture-audit-layer.png` 均为 400 × 400 RGBA PNG。

## 02 Key Metrics

- 状态：响应式结构框架与正式 3D 图标已接入，等待文案确认和像素级精修。
- 实现文件：`index.html`、`style.css`。
- 已实现：四张语义化指标卡、桌面四列、平板两列、手机单列布局，以及四张正式透明 3D 配图。
- 视觉调整：Hero、指标区与管理问题区共享 `main` 的统一底色；模块 02、03 不再设置独立背景渐变，避免区块交界处出现明显分割线。四组指标数字统一使用 DIN 字体栈与蓝色。
- 卡片排版：左侧独立文案容器包含标题、指标和说明，右侧为独立配图区，两区间距固定为 `12px`；配图垂直居中，说明文案根据左侧容器宽度自适应换行。
- 间距规范：指标卡四边内边距统一为 `24px`；左侧标题到数字、数字到说明文案的垂直间距均为 `24px`。
- 指标排版：普通指标数字统一为 `48px`，核算周期为 `40px`；核算周期在所有断点保持单行，说明文案在左侧文案容器内自然换行。
- 指标事实：采购响应 `26h`、成本降低 `2.1%`、核算周期 `月→天`、BIM 预测 `40%` 均可从参考图确认。
- 实现推断：四张卡片的说明文案因参考图清晰度有限，当前按可辨认语义补写，待用户确认。
- 正式配图：`metric-purchase-response.png`、`metric-cost-reduction.png`、`metric-accounting-cycle.png`、`metric-bim-prediction.png`，均为 500 × 500 RGBA PNG。

## 03 Management Problems

- 状态：响应式结构框架与正式配图已完成，等待文案确认和像素级精修。
- 实现文件：`index.html`、`style.css`。
- 已实现：居中标题、进度/材料/成本/协同四张语义化问题卡、四张正式透明配图，以及桌面四列、平板两列、手机单列布局。
- 间距规范：卡片四边内边距统一为 `16px`；图片与文案间距统一为 `8px`；标题与说明文案间距统一为 `4px`。
- 高度规则：卡片不设置固定高度或最小高度，由配图、文案和内边距自适应撑开；同一栅格行保持自然等高。
- 配图尺寸：四张问题图标在所有断点统一显示为 `60px × 60px`。
- 视觉事实：区块主标题、四类黑箱名称、四列卡片结构、左图标右文案关系可从参考图确认。
- 实现推断：卡片说明文字因参考图清晰度有限，当前按可辨认内容和业务语义补写，待用户确认。
- 正式配图：`problem-schedule.png`、`problem-material.png`、`problem-cost.png`、`problem-collaboration.png`，均为 500 × 500 RGBA PNG。
