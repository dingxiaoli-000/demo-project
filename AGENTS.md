# 项目工作约定

## 唯一视觉事实源

- 唯一视觉事实源是 `reference/site-reference.jpg`。
- 原图尺寸：2876 × 8752 px。
- SHA-256：`a39dac4287deeb97196c12257ba25bc85d06f977804716dc36b4742c1e7e5632`。
- 所有布局、颜色、层级、文案、构图和素材风格判断均以该图片为准。
- 未经用户明确确认，不得用生成图、临时截图或实现结果替换事实源。
- 对原图无法确认的信息必须记录为“待确认”或“实现推断”，不能当作事实。

## 开始工作前

1. 阅读 `docs/PROJECT_PLAN.md`。
2. 阅读 `docs/DECISIONS.md`。
3. 检查事实源文件及其 SHA-256。
4. 只处理当前阶段范围，不提前固化未经确认的视觉细节。

## 实现原则

- 页面必须是独立、响应式官网，不使用整张效果图作为页面背景。
- 文本、按钮、导航、卡片、数据和时间轴用真实 HTML/CSS 实现。
- 简单图标、轨道和装饰优先使用 SVG。
- 复杂 3D 插画和场景图单独高清重制，并保留生成记录与版本。
- 桌面端以事实源为像素级比对基准；移动端是在相同设计语言下的响应式延展。
- 每个阶段完成后更新项目记录和验收状态。

